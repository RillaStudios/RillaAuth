import 'package:flutter/material.dart';

class DbSettingScreen extends StatelessWidget {
  const DbSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
