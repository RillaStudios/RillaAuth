import 'package:flutter/material.dart';

class AuthSettingScreen extends StatelessWidget {
  const AuthSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
